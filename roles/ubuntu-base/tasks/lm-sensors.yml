---
- name: install lm-sensors package
  ansible.builtin.apt:
    update_cache: true
    state: latest
    pkg:
      - lm-sensors

- name: detect sensors
  ansible.builtin.command: sensors-detect --auto
